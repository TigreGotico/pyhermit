name: Type Check

on:
  pull_request:
    branches: [dev, master]
  workflow_dispatch:

jobs:
  type_check:
    uses: OpenVoiceOS/gh-automations/.github/workflows/type-check.yml@dev
    with:
      mypy_args: 'src/hermit --strict'
      test_extras: 'dev,owlready2'
      fail_on_errors: true
